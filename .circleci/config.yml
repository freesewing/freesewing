version: 2
jobs:
  build:
    docker:
      - image: circleci/node:lts
    steps:
      - checkout
      - run: 'sudo npm install -g yarn@latest lerna@latest'
      - run: yarn install
      - run: lerna run build
