const defaultSettings = {
  sa: 0,
  saBool: false,
  saMm: 10,
  complete: true,
  paperless: false,
  units: 'metric',
  locale: 'en',
  margin: 2,
  renderer: 'react',
  embed: true,
}

export default defaultSettings
