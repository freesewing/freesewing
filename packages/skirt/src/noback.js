export default function (part) {
    let { Point, points, Path, paths, measurements, options } = part.shorthand();

// Complete?
if (complete) {
    if (sa) {
    }
    // Paperless?
    if (paperless) {
    }
}
return part;
}