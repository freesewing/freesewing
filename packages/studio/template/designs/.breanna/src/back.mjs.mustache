import { front as breannaBack } from '@freesewing/breanna'

export const back = {
  from: breannaBack,
  name: '{{ name }}.back',
  hide: { from: true },
  options: {},
  draft: ({ part }) => {
    return part
  },
}
