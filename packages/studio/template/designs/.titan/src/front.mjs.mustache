import { front as titanFront } from '@freesewing/titan'

export const front = {
  from: titanFront,
  name: '{{ name }}.front',
  hide: { inherited: true },
  measurements: [],
  options: {},
  draft: ({ part }) => {
    return part
  },
}
